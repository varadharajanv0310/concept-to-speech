# Groq API key — powers sentence expansion, predictive concept suggestion and
# semantic (description-based) retrieval.
#
# Without it the six aphasia experiences, concept boards, quick phrases and
# browser speech synthesis all still work; only the language-model layer degrades.
GROQ_API_KEY=
